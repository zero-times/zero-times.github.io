# 修复清单 - 2026-02-25

## 审计报告
- 文件: /Users/mac/Documents/GitHub/blog/reports/site_audit_20260225_140432.json
- 总分: 8.8/10

## 本次改动（2 项）
### 性能
- [x] 将 `_layouts/default.html` 中 Formspree 的 `preconnect/dns-prefetch` 触发条件从「`/contact/` 或 `/blog/`」收敛到仅 `contact` 页面，减少博客列表页不必要的第三方连接开销（移动端首屏更友好）。

### 审计能力（断链/SEO/移动端）
- [x] 新增 `scripts/site_audit_tool.py`，可自动输出 `reports/site_audit_YYYYMMDD_HHMMSS.json`，覆盖：
  - 布局/移动端基础检查（viewport、skip-link）
  - 链接模式检查（重复协议、占位域名）
  - SEO 基线检查（`_config.yml` 关键字段、`{% seo %}`）
  - 性能卫生检查（第三方 preconnect 条件）

## 验证
- [x] `python3 scripts/site_audit_tool.py`
- [x] `rg -n "formspree.io|page.url contains /contact/" _layouts/default.html`
- [x] `python3 scripts/site_audit_tool.py` 输出报告总分 `8.8/10`，断链扫描 `Clean`。

## 下一步建议
- 下一轮可将 `site_audit_tool.py` 增加可选 HTTP 状态检查（仅对新增链接做抽样），进一步提升断链检测精度。
