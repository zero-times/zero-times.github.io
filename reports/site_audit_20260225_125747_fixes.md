# 修复清单 - 2026-02-25

## 审计报告
- 文件: /Users/mac/Documents/GitHub/blog/reports/site_audit_20260225_125747.json
- 总分: 8.6/10

## 修复项
### 性能
- [x] 将 `_layouts/default.html` 的页面特色图 preload 改为条件触发：仅在 `post` 或显式高优先级图片时预加载，避免 lazy 图片占用关键渲染带宽。

### 断链检查
- [x] 对 `_posts`、`_blogs`、`pages`、`_layouts`、`_includes` 执行链接模式扫描（重复协议、占位域名、localhost），本次未发现新增明显断链。

## 验证
- [x] `rg -n "https?://[^\s\)]*https?://" _posts _blogs pages _layouts _includes index.html`
- [x] `rg -n "example\.com|localhost|127\.0\.0\.1|TODO|TBD" _posts _blogs pages _layouts _includes index.html _config.yml`

## 下一步建议
- 为链接检查补一个可重复执行的脚本（例如输出 JSON 的链接体检脚本）并接入定时任务。
